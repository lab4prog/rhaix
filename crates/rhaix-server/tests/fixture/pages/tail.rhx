---
page.title = "Хвіст";

if req.query("mode") == "return" {
    return "готове тіло";
}

// Останній вираз файлу навмисно щось повертає — і це не має поїхати клієнту.
db.count("notes");
---
<p>розмітка на місці</p>
