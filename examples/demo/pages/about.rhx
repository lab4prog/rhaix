---
page.title = "Про фреймворк";
let secret = "не має потрапити в HTML";
let started = now();
---
<div @if={req.is_htmx} @oob={"#nav"}><Nav /></div>

<h1>Про фреймворк</h1>
<p>Файл має frontmatter, але у відповіді його немає.</p>
<p>Заголовок сторінки, який побачив layout: <code>{{ page.title }}</code></p>
